spring.application.name=samkash-farm-api

spring.profiles.default=dev

server.port=${PORT:8080}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=${JPA_SHOW_SQL:false}

spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata
spring.jackson.time-zone=Asia/Kolkata

spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:3}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:0}
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT_MS:30000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME_MS:300000}
spring.datasource.hikari.keepalive-time=${DB_KEEPALIVE_TIME_MS:0}

app.security.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.security.jwt.invalidate-on-restart=${JWT_INVALIDATE_ON_RESTART:true}

app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:}
app.bootstrap.admin.name=${BOOTSTRAP_ADMIN_NAME:Samaksh Farms Admin}
app.bootstrap.admin.enabled=${BOOTSTRAP_ADMIN_ENABLED:false}

app.mail.enabled=${MAIL_ENABLED:false}
app.google.client-id=${GOOGLE_CLIENT_ID:}
app.database.repair-on-startup=${DB_REPAIR_ON_STARTUP:false}
app.public-content.dynamic-enabled=${PUBLIC_DYNAMIC_CONTENT_ENABLED:false}

spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:25}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:false}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:false}
